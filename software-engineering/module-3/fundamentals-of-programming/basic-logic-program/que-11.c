// Find circumference of Square. Formula : C = 4 * a

#include <stdio.h>

main() {

	float a, c;
	
	
    printf("Enter the length of one side of the Square: ");
	scanf("%f", &a);
	
	c = 4 * a;
		
    printf("\nThe circumference of the Square is: %.2f", c);
	
}
